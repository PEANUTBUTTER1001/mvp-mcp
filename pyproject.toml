[project]
name = "mvp-mcp"
version = "0.1.0"
description = "MVP 설계 전문 MCP 서버 — 한 줄 요청을 구현 가능한 프로젝트 명세로 변환"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = [
    "mcp[cli]>=1.2.0",
    "pydantic>=2.6",
    "pydantic-settings>=2.2",
]

[project.scripts]
mvp-mcp = "mvp_mcp.main:main"

[dependency-groups]
dev = [
    "ruff>=0.4",
    "black>=24.0",
    "mypy>=1.10",
    "pytest>=8.0",
    "import-linter>=2.13",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mvp_mcp"]

[tool.ruff]
line-length = 100
target-version = "py311"
src = ["src", "tests"]

[tool.ruff.lint]
# E/F(pyflakes,pycodestyle), I(isort), UP(pyupgrade), B(bugbear)
select = ["E", "F", "I", "UP", "B"]

[tool.black]
line-length = 100
target-version = ["py311"]

[tool.mypy]
python_version = "3.11"
strict = true
mypy_path = "src"
packages = ["mvp_mcp"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

# 실행 가능한 아키텍처 가드레일: 계층 의존 방향을 강제한다.
# 낮은 레이어(아래)는 높은 레이어(위)를 import 할 수 없다.
#   presentation → data → domain → core  (역방향 import는 계약 위반)
# 이 계약은 CI(lint-imports)와 tests/test_guardrails.py 양쪽에서 검증된다.
[tool.importlinter]
root_package = "mvp_mcp"

[[tool.importlinter.contracts]]
name = "레이어 방향 강제 (Clean Architecture)"
type = "layers"
layers = [
    "mvp_mcp.presentation",
    "mvp_mcp.data",
    "mvp_mcp.domain",
    "mvp_mcp.core",
]
